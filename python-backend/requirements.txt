# Python 백엔드 인덱싱 엔진 의존성

# Web Framework
Flask==3.0.0
Flask-CORS==4.0.0

# 텍스트 추출
chardet==5.2.0

# 문서 파일 파싱
python-docx==1.1.0        # .docx 지원
python-pptx==0.6.23       # .pptx 지원
PyMuPDF==1.23.8          # .pdf 지원 (fitz) - 빠른 PDF 파싱
openpyxl==3.1.2          # .xlsx 지원
xlrd==2.0.1              # .xls 대체 지원 (Excel 설치 불필요!)
pywin32==306             # .doc, .ppt, .xls, .hwp 지원 (Windows 전용)
olefile==0.47            # .hwp 대체 지원 (제한적)
pyhwp==0.1b15            # .hwp 지원 (한글 설치 불필요!)

# GUI (선택적)
PyQt6==6.6.1             # PyQt6 GUI
requests==2.31.0         # Flask API 통신

# 유틸리티
Werkzeug==3.0.1

# 사용자 입력 감지 (키보드/마우스)
pynput==1.7.6

# 텍스트 요약 (TextRank)
sumy==0.11.0
nltk==3.8.1
numpy==1.24.3            # sumy 의존성

# OCR (광학 문자 인식) - 이미지/스캔 PDF 처리
pytesseract==0.3.10      # Tesseract OCR Python wrapper
# Note: Tesseract OCR 엔진을 별도로 설치해야 합니다
# 다운로드: https://github.com/UB-Mannheim/tesseract/wiki
# 설치 후 환경변수나 코드에서 경로 설정 필요

# 이미지 캡셔닝 (AI 이미지 묘사) - 로컬 AI 모델
transformers==4.36.2     # Hugging Face Transformers (BLIP 모델)
torch==2.1.2             # PyTorch (CPU 버전)
torchvision==0.16.2      # PyTorch Vision (이미지 처리)
# Note: 첫 실행 시 BLIP 모델 자동 다운로드 (약 1-2GB)
# GPU가 있으면 훨씬 빠르지만 CPU에서도 작동합니다

# GPT 요약 (OpenAI API)
openai==1.54.0          # OpenAI Python SDK
# Note: OpenAI API Key 필요 (https://platform.openai.com/api-keys)

